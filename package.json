{
  "name": "hybridathlete",
  "version": "0.0.1",
  "private": true,
  "description": "Offline-first adaptive training planner PWA (German UI). Evidence-based replanning suggestions for a multi-sport week.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.0",
    "fake-indexeddb": "^6.0.0",
    "vitest": "^3.2.0"
  }
}
