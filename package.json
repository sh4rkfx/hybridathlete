{
  "name": "hybridathlete",
  "version": "0.0.1",
  "private": true,
  "description": "Offline-first adaptive training planner PWA (German UI). Evidence-based replanning suggestions for a multi-sport week.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run --coverage --reporter=default --reporter=json --outputFile=.vitest-report.json && node scripts/build-report.mjs",
    "test:watch": "vitest",
    "coverage": "npm test"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.0",
    "fake-indexeddb": "^6.0.0",
    "vitest": "^3.2.0"
  }
}
